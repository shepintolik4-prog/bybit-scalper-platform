services:
  # Самостоятельный стек для реалистичного теста.
  # Запуск: docker compose -f docker-compose.realistic.yml -p bybit-scalper-realistic up -d --build

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: scalper
      POSTGRES_PASSWORD: scalper
      POSTGRES_DB: scalper
    ports:
      - "5434:5432"
    volumes:
      - pgdata_realistic:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U scalper -d scalper"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env.realistic
    environment:
      MODEL_DIR: /app/models
      DATABASE_URL: postgresql+psycopg2://scalper:scalper@db:5432/scalper
    ports:
      - "8001:8000"
    volumes:
      - ./backend/app:/app/app
      - ./backend/models:/app/models
    depends_on:
      db:
        condition: service_healthy
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5174:80"
    depends_on:
      - backend

volumes:
  pgdata_realistic:

